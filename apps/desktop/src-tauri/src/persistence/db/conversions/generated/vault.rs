//! Generated by ontogen. DO NOT EDIT.

use crate::persistence::db::entities::vault as entity;
use crate::schema::Vault;

impl Vault {
    pub fn from_model(model: &entity::Model) -> Self {
        Self {
            id: model.id.clone(),
            name: model.name.clone(),
            path: model.path.clone(),
            config_path: model.config_path.clone(),
            watch_enabled: model.watch_enabled,
            schedule: model.schedule.clone(),
            created_at: model.created_at.clone(),
            updated_at: model.updated_at.clone(),
        }
    }

    pub fn to_active_model(&self) -> entity::ActiveModel {
        use sea_orm::Set;

        entity::ActiveModel {
            id: Set(self.id.clone()),
            name: Set(self.name.clone()),
            path: Set(self.path.clone()),
            config_path: Set(self.config_path.clone()),
            watch_enabled: Set(self.watch_enabled),
            schedule: Set(self.schedule.clone()),
            created_at: Set(self.created_at.clone()),
            updated_at: Set(self.updated_at.clone()),
        }
    }
}
