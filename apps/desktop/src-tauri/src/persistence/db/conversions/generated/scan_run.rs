//! Generated by ontogen. DO NOT EDIT.

use crate::persistence::db::entities::scan_run as entity;
use crate::schema::ScanRun;

impl ScanRun {
    pub fn from_model(model: &entity::Model) -> Self {
        Self {
            id: model.id.clone(),
            vault_id: model.vault_id.clone(),
            started_at: model.started_at.clone(),
            finished_at: model.finished_at.clone(),
            trigger: model.trigger.clone(),
            status: model.status.clone(),
            error_count: model.error_count,
            warn_count: model.warn_count,
            report_count: model.report_count,
            error_message: model.error_message.clone(),
        }
    }

    pub fn to_active_model(&self) -> entity::ActiveModel {
        use sea_orm::Set;

        entity::ActiveModel {
            id: Set(self.id.clone()),
            vault_id: Set(self.vault_id.clone()),
            started_at: Set(self.started_at.clone()),
            finished_at: Set(self.finished_at.clone()),
            trigger: Set(self.trigger.clone()),
            status: Set(self.status.clone()),
            error_count: Set(self.error_count),
            warn_count: Set(self.warn_count),
            report_count: Set(self.report_count),
            error_message: Set(self.error_message.clone()),
        }
    }
}
