//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::OpenerPreference;
use crate::schema::{CreateOpenerPreferenceInput, UpdateOpenerPreferenceInput};
use crate::store::opener_preference::OpenerPreferenceUpdate;
use crate::store::Store;

/// List all opener_preferences
pub async fn list(store: &Store) -> Result<Vec<OpenerPreference>, AppError> {
    store.list_opener_preferences(None, None).await
}

/// Get a single opener_preference by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<OpenerPreference, AppError> {
    store.get_opener_preference(id).await
}

/// Create a new opener_preference
pub async fn create(
    store: &Store,
    input: CreateOpenerPreferenceInput,
) -> Result<OpenerPreference, AppError> {
    let opener_preference: OpenerPreference = input.into();
    store.create_opener_preference(opener_preference).await
}

/// Update an existing opener_preference
pub async fn update(
    store: &Store,
    id: &str,
    input: UpdateOpenerPreferenceInput,
) -> Result<OpenerPreference, AppError> {
    let updates: OpenerPreferenceUpdate = input.into();
    store.update_opener_preference(id, updates).await
}

/// Delete a opener_preference by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_opener_preference(id).await
}
