//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::ScanRun;
use crate::schema::{CreateScanRunInput, UpdateScanRunInput};
use crate::store::scan_run::ScanRunUpdate;
use crate::store::Store;

/// List all scan_runs
pub async fn list(store: &Store) -> Result<Vec<ScanRun>, AppError> {
    store.list_scan_runs(None, None).await
}

/// Get a single scan_run by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<ScanRun, AppError> {
    store.get_scan_run(id).await
}

/// Create a new scan_run
pub async fn create(store: &Store, input: CreateScanRunInput) -> Result<ScanRun, AppError> {
    let scan_run: ScanRun = input.into();
    store.create_scan_run(scan_run).await
}

/// Update an existing scan_run
pub async fn update(
    store: &Store,
    id: &str,
    input: UpdateScanRunInput,
) -> Result<ScanRun, AppError> {
    let updates: ScanRunUpdate = input.into();
    store.update_scan_run(id, updates).await
}

/// Delete a scan_run by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_scan_run(id).await
}
