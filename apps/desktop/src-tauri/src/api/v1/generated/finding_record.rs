//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::FindingRecord;
use crate::schema::{CreateFindingRecordInput, UpdateFindingRecordInput};
use crate::store::finding_record::FindingRecordUpdate;
use crate::store::Store;

/// List all finding_records
pub async fn list(store: &Store) -> Result<Vec<FindingRecord>, AppError> {
    store.list_finding_records(None, None).await
}

/// Get a single finding_record by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<FindingRecord, AppError> {
    store.get_finding_record(id).await
}

/// Create a new finding_record
pub async fn create(
    store: &Store,
    input: CreateFindingRecordInput,
) -> Result<FindingRecord, AppError> {
    let finding_record: FindingRecord = input.into();
    store.create_finding_record(finding_record).await
}

/// Update an existing finding_record
pub async fn update(
    store: &Store,
    id: &str,
    input: UpdateFindingRecordInput,
) -> Result<FindingRecord, AppError> {
    let updates: FindingRecordUpdate = input.into();
    store.update_finding_record(id, updates).await
}

/// Delete a finding_record by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_finding_record(id).await
}
