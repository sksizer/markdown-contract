//! Generated by ontogen. DO NOT EDIT.

use crate::schema::AppError;
use crate::schema::Vault;
use crate::schema::{CreateVaultInput, UpdateVaultInput};
use crate::store::vault::VaultUpdate;
use crate::store::Store;

/// List all vaults
pub async fn list(store: &Store) -> Result<Vec<Vault>, AppError> {
    store.list_vaults(None, None).await
}

/// Get a single vault by ID
pub async fn get_by_id(store: &Store, id: &str) -> Result<Vault, AppError> {
    store.get_vault(id).await
}

/// Create a new vault
pub async fn create(store: &Store, input: CreateVaultInput) -> Result<Vault, AppError> {
    let vault: Vault = input.into();
    store.create_vault(vault).await
}

/// Update an existing vault
pub async fn update(store: &Store, id: &str, input: UpdateVaultInput) -> Result<Vault, AppError> {
    let updates: VaultUpdate = input.into();
    store.update_vault(id, updates).await
}

/// Delete a vault by ID
pub async fn delete(store: &Store, id: &str) -> Result<(), AppError> {
    store.delete_vault(id).await
}
