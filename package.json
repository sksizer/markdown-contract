{
  "name": "markdown-contract-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bun workspace root for markdown-contract. The publishable library + CLI lives in packages/core; runtime apps (the single-binary prototype, the daemon-web UI prototype) live under apps/; websites, including the documentation site, live under sites/.",
  "license": "MIT",
  "author": "sksizer",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "sites/*"
  ],
  "scripts": {
    "audit": "bun audit --audit-level=high",
    "lint:deps": "knip",
    "metrics": "scc packages/core/src"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@moonrepo/cli": "2.3.5",
    "knip": "^5"
  }
}
