{
  "name": "markdown-contract-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bun workspace root for markdown-contract. The publishable library + CLI lives in packages/core; runtime apps (the M-0009 vault dashboard, the daemon-web UI prototype) live under apps/; canonical examples (the M-0008 single-binary prototype) live under examples/; websites, including the documentation site, live under sites/.",
  "license": "MIT",
  "author": "sksizer",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/*",
    "apps/*",
    "examples/*",
    "sites/*"
  ],
  "scripts": {
    "audit": "bun audit --audit-level=high",
    "lint:deps": "knip",
    "metrics": "scc packages/core/src",
    "prepare": "lefthook install || true"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@moonrepo/cli": "2.3.5",
    "knip": "^5",
    "lefthook": "^2.1.9"
  }
}
