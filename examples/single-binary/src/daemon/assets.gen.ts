/**
 * GENERATED FILE — (re)written by `scripts/gen-assets.ts` after `nuxt generate`.
 *
 * Maps each SPA route/asset pathname to a file the binary can serve. In the
 * committed stub the map is EMPTY: `bun run` serves the SPA from
 * `ui/.output/public/` on disk instead (see `./static.ts`). The generator
 * rewrites this file with one `with { type: "file" }` import per built asset so
 * `bun build --compile` embeds the whole SPA into the executable (T-SPAE: the
 * HTML-import + explicit-file-import technique; whole-directory embedding is
 * avoided per oven-sh/bun#5445). Don't commit the regenerated form.
 */
export const assets: Record<string, string> = {};
