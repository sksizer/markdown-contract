mcVersion: 1
kind: contract
# fixture: v11a — Cell enum violation  (provenance: 11a-cell-enum-violation.ts)

body:
  order: none
  allowUnknown: false
  sections:
    - section: Files to touch
      content:
        table:
          columns:
            - Location
            - Kind
            - Change
          cells:
            Kind:
              enum:
                - add
                - modify
                - delete
