mcVersion: 1
kind: contract
# fixture: v22 — Section-scoped requires  (provenance: 22-text-requires-section.ts)

body:
  order: recognized-relative
  allowUnknown: true
  sections:
    - section: Summary
      requires:
        - pattern: outcome
          note: the decision outcome must be named
