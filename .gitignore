node_modules/
dist/
coverage/
.sdlc/
*.log
.DS_Store
.rumdl_cache/

# --- Claude Code project-local ---
# Default: ignore everything inside .claude/ ...
.claude/*
# ... then explicitly un-ignore the shareable bits.
!.claude/settings.json
!.claude/skills/
!.claude/commands/
!.claude/agents/
!.claude/hooks.json
!.claude/hooks/
!.claude/hooks/**

# --- SDLC plugin project-local ---
.sdlc/*
!.sdlc/skill-ext/
!.sdlc/skill-ext/**

# moon
.moon/cache
.moon/docker

# Obsidian local vault config (docs/ is opened as a personal vault)
docs/.obsidian/
# Obsidian "Bases" view definitions (personal, generated locally)
*.base

# bun lockfile — repo is npm-primary (package-lock.json); see M-0005
bun.lock
