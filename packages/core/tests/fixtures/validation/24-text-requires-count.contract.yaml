mcVersion: 1
kind: contract
# fixture: v24 — Section-scoped requires with a min count  (provenance: 24-text-requires-count.ts)

body:
  order: recognized-relative
  allowUnknown: true
  sections:
    - section: Checklist
      requires:
        - pattern: DONE
          min: 2
          note: every step must be marked DONE
