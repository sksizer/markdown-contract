mcVersion: 1
kind: contract
# fixture: v25 — Section-scoped requires via a regex alternation  (provenance: 25-text-regex.ts)

body:
  order: recognized-relative
  allowUnknown: true
  sections:
    - section: Failure modes
      requires:
        - regex: "LEASE-(CONFLICT|MISSING) ref="
          note: lease failure markers
