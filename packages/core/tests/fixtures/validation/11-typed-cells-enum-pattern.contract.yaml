mcVersion: 2
kind: contract
# fixture: v11 — Typed cells: enum / pattern  (provenance: 11-typed-cells-enum-pattern.ts)

body:
  order: none
  additionalSections: false
  sections:
    - section: Files
      content:
        table:
          columns:
            - File
            - Kind
            - Location
          cells:
            Kind:
              enum:
                - add
                - modify
                - delete
            Location:
              type: string
              pattern: '^[A-Za-z0-9._\/-]+\/$'
