mcVersion: 2
kind: contract
# fixture: v17 — Node-level custom rule  (provenance: 17-node-level-custom-rule.ts)

body:
  order: recognized-relative
  additionalSections: true
  sections:
    - section: Summary
      # GAP: rule("summary/mentions-outcome", fn) — node-level custom rule (rule(id, fn)) is not expressible in v1; checked behavior (Summary prose must contain "outcome") is dropped
