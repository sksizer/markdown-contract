---
id: D-0002
status: open/draft
title: A decision missing a required section
---

# A decision missing a required section

## Context

This document declares a bad `status` (outside the enum, an error-level frontmatter
finding) and is missing the required `## Decision` section (a structure error). Two
error-level findings, so the corpus run must fail on it.
