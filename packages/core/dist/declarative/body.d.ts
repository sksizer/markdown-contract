import type { SectionSeq } from "../core/types.js";
/** Compile a `body:` mapping into a `sections(opts, specs)` body grammar. */
export declare function compileBody(node: unknown, path?: string): SectionSeq;
//# sourceMappingURL=body.d.ts.map